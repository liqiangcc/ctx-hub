# POC CI Policy

This POC branch uses strict Rust CI gates.

Required checks include formatting, compile checks, clippy warnings as errors, tests, and release build.

The POC should remain a Draft PR until all required checks pass on Linux, macOS, and Windows.
